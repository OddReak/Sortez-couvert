/**
 * Placeholder de Phase 0.
 *
 * L'interface produit (layout 15/60/25, globe, bague) est construite à partir
 * de la Phase 2 — voir docs/PROMPT-MAITRE.md §16. Ce composant existe
 * uniquement pour prouver que la chaîne Vite + React + Tailwind fonctionne.
 */
export function App() {
  return (
    <main className="flex min-h-dvh flex-col items-center justify-center gap-2 p-4 text-center">
      <h1 className="text-2xl font-bold">Terra</h1>
      <p className="text-sm opacity-70">
        Phase 0 — Fondations. L’interface arrive en Phase 2.
      </p>
    </main>
  );
}
