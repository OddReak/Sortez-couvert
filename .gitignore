node_modules
dist
dist-ssr
*.local

# env — seuls .env.example et .env.mock (non secret) sont versionnés
.env
.env.*
!.env.example
!.env.mock

# tests & couverture
coverage
playwright-report
test-results
blob-report
playwright/.cache

# sorties brutes de la sonde Foreca (locales, régénérables) — les fixtures
# curées vivent dans src/mocks/fixtures/
tests/fixtures/probe/

# build / outils
*.tsbuildinfo
.eslintcache
stats.html
.vercel

# OS / éditeurs
.DS_Store
*.log
npm-debug.log*
pnpm-debug.log*
.idea
